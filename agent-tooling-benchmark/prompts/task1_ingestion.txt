You have the following files in the current directory:
- london_h3_grid.parquet: An H3 hexagonal grid of Greater London with geospatial
  features for retail site selection. Each row is a hexagonal cell.
- cleaned_census.csv: ONS Census 2021 demographic data.

Please perform the following:
1. Load both datasets and print their shapes and column names.
2. Print the data types of all columns in the parquet file. Identify any
   columns where the dtype may not match the semantic meaning (e.g., a
   categorical ID stored as float).
3. Identify and report all columns with missing values, showing count and
   percentage, sorted descending.
4. Check for zero-variance columns (features that have the same value for
   all rows) and report them.
5. Verify spatial integrity: check that all H3 cell IDs are valid and at
   the same resolution.
6. For any data quality issues found, recommend and implement fixes.
7. Save a cleaned version as london_h3_cleaned.parquet.

Show all code and print all outputs.
