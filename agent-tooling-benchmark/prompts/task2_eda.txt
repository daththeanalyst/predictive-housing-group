You have london_h3_grid.parquet in the current directory. It contains an H3
hexagonal grid of Greater London with features including: population density,
demographics (age, employment, qualifications), crime statistics, transport
accessibility, graph centrality metrics, and POI (point of interest) counts
for 6 business types (cafe, restaurant, pub, fast_food, gym, bakery).

Please perform exploratory data analysis:
1. Generate summary statistics for all numeric features.
2. Plot the distribution of population across hexagons and comment on its
   shape (skewness, zero-inflation).
3. Create a correlation heatmap of the top 15 most inter-correlated features.
4. Generate scatter plots showing the relationship between population,
   property_crime_log1p, and n_cafe (POI count for cafes).
5. Compare feature distributions across hexagons that have cafes (n_cafe > 0)
   versus those that don't (n_cafe == 0) — plot at least 3 key features.
6. Identify and report the 3 most surprising or actionable insights that
   would inform a site-selection model.

Save all plots as PNG files. Show all code and print all outputs.
