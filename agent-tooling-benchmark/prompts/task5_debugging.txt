The file broken_pipeline.py in the current directory contains a machine learning
pipeline for predicting cafe presence in London H3 hexagons using the
london_h3_grid.parquet dataset.

The pipeline has multiple bugs that affect the correctness and validity of the
results. Please:
1. Read and analyse the code carefully.
2. Identify ALL bugs, explaining why each is a problem.
3. Fix each bug with appropriate corrections.
4. Run the corrected pipeline and report the corrected metrics.
5. Explain how each bug would have affected the reported results (e.g.,
   inflated AUC, misleading F1, etc.).

Show the corrected code in full and print all outputs.
