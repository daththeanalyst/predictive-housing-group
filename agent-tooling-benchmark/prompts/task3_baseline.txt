You have london_h3_grid.parquet in the current directory. Each row is an H3
hexagon in Greater London. The task is binary classification: predict whether
a hexagon contains at least one cafe (create target: 1 if n_cafe > 0, else 0).

Features to use (exclude all n_* POI count columns as they would be leakage):
population, employed_total_perc, age_16_to_34_perc, level4_perc, retired_perc,
no_qualifications_perc, violent_crime_log1p, property_crime_log1p,
antisocial_behaviour_log1p, dist_to_nearest_station_log, station_count_800m,
bus_stop_count_log, betweenness_centrality, closeness_centrality,
eigenvector_centrality, pagerank.

Please:
1. Prepare the data: create the binary target, select the features listed
   above, handle any missing values.
2. Create a train/test split (80/20, random_state=42).
3. Train a Logistic Regression baseline.
4. Train a Random Forest (n_estimators=100, random_state=42).
5. Evaluate both using AUC-ROC, F1-score, precision, and recall.
6. Plot ROC curves for both models on the same figure.
7. Print a comparison table and discuss which model is better and why.

Note: This is a spatial dataset — discuss whether a random train/test split
is appropriate or if spatial cross-validation would be more rigorous.

Use random_state=42 wherever applicable. Show all code and print all outputs.
